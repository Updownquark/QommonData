QommonsData
====
This is intended to be a library that will make it very easy to define a relational data schema via simple java interfaces
(or potentially other methods), load, persist, and modify data for that schema, and migrate the data efficiently when the schema changes.

TODO
